# Required for live prices on the home dashboard; without it the price
# cards stay on placeholders. Market-cap access requires an extended key.
VITE_VYNTREX_API_KEY=
VITE_VYNTREX_MARKET_CAP_API_KEY=
